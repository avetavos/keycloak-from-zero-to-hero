---
title: 'Data Sources'
description: 'วิธีเชื่อมต่อ GrowthBook กับ data warehouse และเขียน experiment assignment query'
sidebar:
  order: 2
---

import Callout from '../../../../components/Callout.astro';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';

## Data Source คืออะไร?

Data Source คือการเชื่อมต่อแบบ read-only จาก GrowthBook ไปยัง warehouse หรือ database ของคุณ GrowthBook ใช้การเชื่อมต่อนี้เพื่อรัน analysis queries เมื่อต้องการ — ไม่มีการเขียนข้อมูลกลับไปเลย Engine ที่รองรับได้แก่ BigQuery, Snowflake, Postgres, MySQL, ClickHouse, Redshift, Databricks, Athena, PrestoDB และอื่นๆ

## การเชื่อมต่อ data source

### ขั้นตอนที่ 1 — เปิด Data Sources

1. เข้าสู่ระบบ GrowthBook
2. ใน sidebar ด้านซ้าย ไปที่ **Analysis** แล้วคลิก **Data Sources**
3. คลิก **Add Data Source**

### ขั้นตอนที่ 2 — เลือกประเภท connection

1. เลือกประเภท warehouse ของคุณ (เช่น **BigQuery**, **Snowflake**, หรือ **Postgres**)
2. กรอกรายละเอียด connection ที่ warehouse ต้องการ:
   - **BigQuery:** วาง service account JSON key และเลือก default project
   - **Snowflake:** กรอก account, warehouse, database, schema, username, และ password
   - **Postgres / MySQL:** กรอก host, port, database name, username, และ password
3. คลิก **Test Connection** เพื่อตรวจสอบว่า GrowthBook เข้าถึง warehouse ได้
4. คลิก **Save**

### ขั้นตอนที่ 3 — เขียน experiment assignment query

Assignment query เป็น query ที่สำคัญที่สุดใน GrowthBook มันตอบคำถามที่ว่า: _"ผู้ใช้คนไหนเห็น experiment variation ไหน และเมื่อไหร่?"_

Assignment query ปกติเลือก 4 columns:

| Column | สิ่งที่เก็บไว้ |
|---|---|
| `experiment_id` | GrowthBook experiment key (เช่น `checkout-redesign`) |
| `variation_id` | ดัชนี variation ที่ผู้ใช้ถูก assign ให้ (0 = control, 1 = variation A, ...) |
| `user_id` | user identifier ที่คงที่ (user UUID, anonymous ID ฯลฯ) |
| `timestamp` | เวลาที่ assignment เกิดขึ้น (UTC) |

ตัวอย่างสำหรับ BigQuery events table:

```sql
SELECT
  experiment_id,
  variation_id,
  user_id,
  timestamp
FROM
  `myproject.analytics.experiment_viewed`
WHERE
  timestamp >= '{{ startDate }}'
  AND timestamp <= '{{ endDate }}'
```

GrowthBook จะแทนที่ `{{ startDate }}` และ `{{ endDate }}` โดยอัตโนมัติเมื่อรัน analysis

### ขั้นตอนที่ 4 — (ไม่บังคับ) เพิ่ม identifier joins

ถ้า warehouse ของคุณใช้ ID ประเภทต่างกัน (anonymous cookie ID กับ logged-in user ID) ให้เพิ่ม **Identifier Joins** เพื่อให้ GrowthBook เชื่อม ID เหล่านั้นกันได้ข้ามตาราง assignment และ metric

## ยังไม่มี warehouse? ไม่เป็นไร

ถ้าคุณเพิ่งเริ่มต้นและยังไม่มี data warehouse GrowthBook สามารถทำงานได้โดยไม่ต้องมี warehouse สำหรับ feature flags และ targeting พื้นฐาน คุณเพิ่ม data source ได้ภายหลังเมื่อพร้อมรัน statistical experiments GrowthBook ยังรองรับการ upload CSV data สำหรับการทดลองแบบเบาๆ ด้วย

<Callout title="Assignment query คือรากฐานของทุกอย่าง">
Experiment assignment query บอก GrowthBook ว่าผู้ใช้คนไหนเห็น variation ไหน และเมื่อไหร่ metric ทุกตัวและผล statistical ทั้งหมดมาจาก query นี้ — ถ้ามันผิดหรือไม่ครบถ้วน การวิเคราะห์ทั้งหมดจะผิดด้วย ตรวจสอบด้วย SQL preview โดยตรงก่อน save
</Callout>

export const quizDataSources = [
  {
    q: 'GrowthBook ทำอะไรกับข้อมูลใน warehouse ของคุณ?',
    options: [
      'คัดลอก events ไปยัง database ของตัวเองเพื่อ query ที่เร็วขึ้น',
      'รัน SQL queries แบบ read-only กับ warehouse ของคุณเมื่อต้องการ',
      'stream events แบบ real time ผ่าน webhook',
      'เก็บ raw events ไว้ใน GrowthBook Cloud',
    ],
    answer: 1,
  },
  {
    q: 'experiment assignment query ต้องคืน 4 columns อะไรบ้าง?',
    options: [
      'user_id, feature_key, value, timestamp',
      'experiment_id, variation_id, user_id, timestamp',
      'session_id, event_name, properties, timestamp',
      'cohort_id, bucket, user_id, created_at',
    ],
    answer: 1,
  },
  {
    q: '{{ startDate }} และ {{ endDate }} ใน assignment SQL คืออะไร?',
    options: [
      'JavaScript template literals ที่ต้อง escape',
      'GrowthBook template variables ที่ถูกแทนที่โดยอัตโนมัติตอน query',
      'SQL comments ที่ถูกตัดออกก่อน execute',
      'ชื่อ column ใน events table ของคุณ',
    ],
    answer: 1,
  },
  {
    q: 'ควรทำอะไรก่อน save Data Source connection ใหม่?',
    options: [
      'สร้าง metric อย่างน้อยหนึ่งตัวก่อน',
      'คลิก Test Connection เพื่อตรวจสอบว่า GrowthBook เข้าถึง warehouse ได้',
      'รัน experiment เต็มรูปแบบก่อน',
      'export schema เป็น CSV',
    ],
    answer: 1,
  },
];

<Quiz client:visible id="metrics-data/data-sources" questions={quizDataSources} />

<ProgressTracker client:visible id="metrics-data/data-sources" />
