---
title: Refresh และ Logout
description: 'จัดการการ refresh token ด้วย keycloak.updateToken() ทำความเข้าใจ session lifetimes และ logout อย่างถูกต้องผ่าน Keycloak end-session endpoint'
sidebar:
  order: 4
---

import Callout from '../../../../components/Callout.astro';
import CopyCode from '../../../../components/CopyCode.tsx';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';

## อายุของ token และ session

Keycloak จัดการแนวคิดสามอย่างที่เกี่ยวข้องกันซึ่งควบคุมระยะเวลาที่ผู้ใช้ยังคง login อยู่:

| แนวคิด | ค่าเริ่มต้นทั่วไป | ควบคุมอะไร |
|---|---|---|
| Access token lifespan | 5 นาที | ระยะเวลาที่ JWT มีผลก่อน backend จะปฏิเสธ |
| Refresh token lifespan | 30 นาที (SSO Session Idle) | ระยะเวลาที่ refresh token สามารถใช้รับ access token ใหม่ |
| SSO session max | 10 ชั่วโมง | ระยะเวลา session สูงสุดโดยสมบูรณ์ แม้มีการ refresh อยู่ |

เมื่อ access token หมดอายุ frontend ควรใช้ refresh token เพื่อรับ token ใหม่แบบ silent โดยไม่บังคับให้ผู้ใช้ login ใหม่ เมื่อ SSO session หมดอายุ ผู้ใช้ต้อง authenticate ใหม่

## การ refresh token ด้วย updateToken

`keycloak.updateToken(minValidity)` คือวิธีที่เหมาะสมในการจัดการ refresh ใน keycloak-js เรียกใช้ก่อน API request ใดก็ตาม ถ้า access token ปัจจุบันจะหมดอายุภายใน `minValidity` วินาที keycloak-js จะแลก refresh token เพื่อรับ access token ใหม่แบบ silent:

export const refreshCode = `// Refresh the token if it expires within 60 seconds,
// then call the API with the fresh token.
async function callApi(path) {
  try {
    await keycloak.updateToken(60);
  } catch {
    // Refresh token has expired — force re-login.
    keycloak.login();
    return;
  }

  const response = await fetch(\`https://api.example.com\${path}\`, {
    headers: { Authorization: \`Bearer \${keycloak.token}\` },
  });
  return response.json();
}`;

<CopyCode client:visible code={refreshCode} label="Copy refresh snippet" />

ถ้า `updateToken` reject (refresh token หมดอายุหรือถูกเพิกถอน) แสดงว่า session ของผู้ใช้หมดแล้ว ควร redirect ไปยัง login ด้วย `keycloak.login()`

## การ logout อย่างถูกต้อง

การเรียกฟังก์ชัน "clear state" ในแอปของคุณเพียงอย่างเดียวไม่เพียงพอ SSO session ของผู้ใช้ยังคงอยู่ใน Keycloak ซึ่งหมายความว่าพวกเขาสามารถเข้าถึงแอปอื่น ๆ ใน realm เดียวกันได้ทันทีโดยไม่ต้อง authenticate ใหม่

การ logout ที่ถูกต้องต้องกดไปที่ end-session endpoint ของ Keycloak `keycloak-js` ทำสิ่งนี้ให้คุณ:

export const logoutCode = `// Redirect to Keycloak's end-session endpoint,
// then back to your app's home page.
keycloak.logout({ redirectUri: 'https://app.example.com/' });`;

<CopyCode client:visible code={logoutCode} label="Copy logout snippet" />

ภายใต้ hood นี้จะ redirect เบราว์เซอร์ไปที่:

```
/realms/{realm}/protocol/openid-connect/logout
  ?id_token_hint=<id_token>
  &post_logout_redirect_uri=https://app.example.com/
```

Keycloak จะล้าง SSO session cookie, ทำเครื่องหมาย refresh token ว่าถูกเพิกถอน และ redirect กลับมายังแอปของคุณ

### สิ่งที่ถูกล้าง

| การกระทำ | ล้าง token ในเครื่อง | ล้าง Keycloak session |
|---|---|---|
| Reset state ในแอปเท่านั้น | ใช่ | ไม่ |
| `keycloak.logout()` | ใช่ | ใช่ |

<Callout title="กดไปที่ end-session endpoint — การ logout เฉพาะในแอปไม่เพียงพอ">
การล้าง state ในแอปของคุณเพียงอย่างเดียวจะลบ token ออกจากหน่วยความจำเท่านั้น SSO session cookie ของ Keycloak ในเบราว์เซอร์ยังคงมีผล จนกว่าจะเรียก keycloak.logout() ผู้ใช้ยังคง login อยู่ทั่วโลกและสามารถกลับเข้าแอปของคุณ (หรือแอปอื่น ๆ ใน realm) โดยไม่ต้องใส่รหัสผ่าน
</Callout>

export const quizRefreshLogout = [
  {
    q: 'keycloak.updateToken(60) ทำหน้าที่อะไร?',
    options: [
      'กำหนดอายุของ access token เป็น 60 วินาที',
      'บังคับให้ผู้ใช้ authenticate ใหม่หากไม่มีการใช้งานเป็นเวลา 60 วินาที',
      'refresh access token แบบ silent หากจะหมดอายุภายใน 60 วินาที',
      'logout ผู้ใช้ออกหลังจาก 60 วินาที',
    ],
    answer: 2,
  },
  {
    q: 'แอปของคุณควรทำอย่างไรเมื่อ keycloak.updateToken() reject (throw)?',
    options: [
      'ลอง refresh ซ้ำสูงสุดสามครั้ง',
      'กลืน error แบบเงียบ ๆ แล้วดำเนินการต่อด้วย token ที่หมดอายุ',
      'เรียก keycloak.login() เพื่อ redirect ผู้ใช้กลับไปยัง Keycloak เพื่อ authenticate ใหม่',
      'จัดเก็บ token ที่ถูกต้องล่าสุดไว้ใน localStorage แล้วนำมาใช้',
    ],
    answer: 2,
  },
  {
    q: 'เหตุใดการล้าง state ของแอปในเครื่องเพียงอย่างเดียวจึงไม่เพียงพอสำหรับการ logout?',
    options: [
      'เพราะ keycloak-js ไม่สามารถล้าง token cache ภายในของมันได้',
      'เพราะ SSO session cookie ของ Keycloak ในเบราว์เซอร์ยังคงมีผลจนกว่าจะเรียก end-session endpoint',
      'เพราะ backend ยังคงถือ access token อยู่',
      'เพราะ refresh token ถูกจัดเก็บใน localStorage โดยค่าเริ่มต้น',
    ],
    answer: 1,
  },
  {
    q: 'SSO Session Idle timeout ควบคุมอะไร?',
    options: [
      'ระยะเวลาที่ access token JWT มีผล',
      'ระยะเวลาที่ session ของ Keycloak admin console คงอยู่',
      'ระยะเวลาที่ refresh token สามารถใช้รับ access token ใหม่ได้โดยไม่ต้องมีการโต้ตอบจากผู้ใช้',
      'ระยะเวลาจนกว่า Keycloak จะ restart',
    ],
    answer: 2,
  },
];

<Quiz client:visible id="securing-apps/refresh-logout" questions={quizRefreshLogout} />

<ProgressTracker client:visible id="securing-apps/refresh-logout" />
