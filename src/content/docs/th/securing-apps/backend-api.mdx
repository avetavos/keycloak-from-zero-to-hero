---
title: Backend API
description: 'ตรวจสอบ JWT บน Node/Express backend (resource server): ดึง JWKS ของ realm ตรวจสอบ signature, issuer, audience และ expiry ด้วยไลบรารี jose'
sidebar:
  order: 3
---

import Callout from '../../../../components/Callout.astro';
import CopyCode from '../../../../components/CopyCode.tsx';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';

## สิ่งที่ resource server ต้องทำ

เมื่อ client ส่ง request พร้อม header `Authorization: Bearer <token>` backend ของคุณต้อง:

1. **ดึง JWKS ของ realm** — ชุด public key ที่ Keycloak ใช้เซ็น JWT URL คือ `<keycloak-url>/realms/<realm>/protocol/openid-connect/certs`
2. **ตรวจสอบ signature** — ยืนยันว่า JWT ถูกเซ็นโดย Keycloak ไม่ใช่ปลอมโดยผู้โจมตี
3. **ตรวจสอบ issuer (`iss`)** — claim `iss` ใน token ต้องเท่ากับ `<keycloak-url>/realms/<realm>` token จาก realm อื่นหรือ server อื่นต้องถูกปฏิเสธ
4. **ตรวจสอบ audience (`aud`)** — claim `aud` ควรมี client ID ของคุณ เพื่อยืนยันว่า token ถูก issue สำหรับ API ของคุณ
5. **ตรวจสอบ expiry (`exp`)** — ปฏิเสธ token ที่หมดอายุแล้ว

หากตรวจสอบล้มเหลวในข้อใดข้อหนึ่ง token นั้นไม่ถูกต้องและต้องปฏิเสธ request ด้วย `401 Unauthorized`

## ตัวอย่าง Node/Express ด้วย jose

ไลบรารี `jose` เป็น JWT library ที่ทันสมัย ไม่มี dependency เสริม รองรับการดึง JWKS และการตรวจสอบมาตรฐานทั้งหมด ติดตั้ง:

```bash
npm install jose
```

export const backendApiCode = `import { createRemoteJWKSet, jwtVerify } from 'jose';
import express from 'express';

const KEYCLOAK_URL = 'http://localhost:8080';
const REALM = 'my-app';
const CLIENT_ID = 'my-app-backend';

// Cache the JWKS remote keyset — jose handles key rotation automatically.
const JWKS = createRemoteJWKSet(
  new URL(\`\${KEYCLOAK_URL}/realms/\${REALM}/protocol/openid-connect/certs\`)
);

async function requireAuth(req, res, next) {
  const authHeader = req.headers['authorization'] ?? '';
  const token = authHeader.startsWith('Bearer ') ? authHeader.slice(7) : null;

  if (!token) {
    return res.status(401).json({ error: 'Missing bearer token' });
  }

  try {
    const { payload } = await jwtVerify(token, JWKS, {
      issuer: \`\${KEYCLOAK_URL}/realms/\${REALM}\`,
      audience: CLIENT_ID,
    });
    req.user = payload; // attach claims to the request
    next();
  } catch (err) {
    return res.status(401).json({ error: 'Invalid or expired token' });
  }
}

const app = express();

app.get('/profile', requireAuth, (req, res) => {
  res.json({
    username: req.user.preferred_username,
    email: req.user.email,
    roles: req.user.realm_access?.roles ?? [],
  });
});

app.listen(3001, () => console.log('API listening on :3001'));`;

<CopyCode client:visible code={backendApiCode} label="Copy Express middleware" />

### โค้ดทำงานอย่างไร

- `createRemoteJWKSet` สร้าง key set ที่ cache ไว้จาก JWKS URL ของ Keycloak เมื่อ Keycloak หมุนเวียน key `jose` จะดึง key ใหม่โดยอัตโนมัติ
- `jwtVerify` ตรวจสอบ signature, `iss`, `aud` และปฏิเสธ token ที่หมดอายุ หากตรวจสอบล้มเหลวจะ throw error — block `catch` จะส่ง `401` กลับ
- หลังการตรวจสอบสำเร็จ `payload` จะมี claims ทั้งหมดที่ถอดรหัสแล้ว (sub, preferred_username, realm_access เป็นต้น) และสามารถนำไปใช้ใน authorisation logic ได้

## การตรวจสอบ roles จาก token

Keycloak เข้ารหัส realm roles ใน `realm_access.roles` และ client-specific roles ใน `resource_access.<clientId>.roles` helper ตรวจสอบ role:

```ts
function hasRole(user: Record<string, unknown>, role: string): boolean {
  const roles = (user.realm_access as { roles?: string[] })?.roles ?? [];
  return roles.includes(role);
}
```

<Callout title="ตรวจสอบทั้งสี่ด้าน: signature, issuer, audience และ expiry">
token ที่มีอยู่เพียงอย่างเดียวไม่ใช่ token ที่ถูกต้อง ผู้โจมตีสามารถสร้าง JWT ที่มี claims ใดก็ได้ มีเพียง cryptographic signature เท่านั้นที่พิสูจน์ว่ามาจาก Keycloak ในทำนองเดียวกัน signature ที่ถูกต้องยังไม่พอ — คุณต้องยืนยันว่า token ถูก issue สำหรับ realm ของคุณ (iss) และมีเจตนาสำหรับ API ของคุณ (aud) และยังไม่หมดอายุด้วย
</Callout>

export const quizBackendApi = [
  {
    q: 'claim iss (issuer) ใน JWT ของ Keycloak บรรจุข้อมูลอะไร?',
    options: [
      'client ID ของแอปพลิเคชัน',
      'username ของผู้ใช้ที่ล็อกอินอยู่',
      'URL ของ Keycloak realm ที่ issue token นั้น',
      'timestamp การหมดอายุในรูปแบบ ISO',
    ],
    answer: 2,
  },
  {
    q: 'จะเกิดอะไรขึ้นหาก claim aud (audience) ไม่ตรงกับ client ID ของ backend?',
    options: [
      'token ถูกยอมรับโดยอัตโนมัติ',
      'Keycloak จะ reissue token ให้โดยอัตโนมัติ',
      'การตรวจสอบ JWT ล้มเหลว และควรส่ง 401 กลับ',
      'token ยังถูกต้องแต่ role จะถูกตัดออก',
    ],
    answer: 2,
  },
  {
    q: 'เหตุใด createRemoteJWKSet จาก jose จึง cache JWKS ไว้?',
    options: [
      'เพื่อหลีกเลี่ยงค่าใช้จ่ายในการเรียก API ของ Keycloak',
      'เพื่อรองรับการหมุนเวียน key — jose จะดึง key ใหม่โดยอัตโนมัติเมื่อ token อ้างถึง key ID ที่ไม่รู้จัก',
      'เพราะ JWKS URL ไม่สามารถเข้าถึงได้แบบสาธารณะ',
      'เพื่อจัดเก็บ token ไว้ในฐานข้อมูล',
    ],
    answer: 1,
  },
  {
    q: 'Keycloak เผยแพร่ public signing key (JWKS) ไว้ที่ใด?',
    options: [
      '/realms/{realm}/protocol/openid-connect/token',
      '/realms/{realm}/protocol/openid-connect/userinfo',
      '/realms/{realm}/protocol/openid-connect/certs',
      '/realms/{realm}/.well-known/openid-configuration',
    ],
    answer: 2,
  },
];

<Quiz client:visible id="securing-apps/backend-api" questions={quizBackendApi} />

<ProgressTracker client:visible id="securing-apps/backend-api" />
