---
title: Production Checklist
description: 'Harden Keycloak สำหรับ production: รัน start ด้วย optimised build, เชื่อมต่อ external database, ตั้ง base URL แบบ hostname v2, terminate HTTPS ให้ถูกต้อง, ให้ session อยู่รอดข้าม restart และใช้ realm export/import สำหรับ config-as-code'
sidebar:
  order: 5
---

import Callout from '../../../../components/Callout.astro';
import CopyCode from '../../../../components/CopyCode.tsx';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';

## จาก start-dev สู่ production

โหมด `start-dev` มีไว้สำหรับการพัฒนาในเครื่องเท่านั้น โหมดนี้ปิด optimised build ใช้ embedded H2 database สร้าง self-signed certificate และผ่อนปรนการตรวจสอบ hostname ค่าเริ่มต้นเหล่านี้ไม่ปลอดภัยใน production เมื่อพร้อม go live มีสี่สิ่งที่คุณต้องจัดการ

## สี่สิ่งที่ต้องมีสำหรับ production

### 1. External database

โดยค่าเริ่มต้น `start-dev` ใช้ embedded H2 database ที่อยู่ภายใน container รีสตาร์ท container แล้วข้อมูลทั้งหมดหาย ใน production ให้ชี้ Keycloak ไปยัง external database — PostgreSQL คือตัวเลือกที่แนะนำ

ตั้งค่า environment variable เหล่านี้:

```
KC_DB=postgres
KC_DB_URL=jdbc:postgresql://db-host:5432/keycloak
KC_DB_USERNAME=keycloak
KC_DB_PASSWORD=<secret>
```

### 2. Fixed hostname

Keycloak ฝัง hostname ของตัวเองใน token issuer, OIDC discovery document และการตรวจสอบ redirect หาก hostname ไม่ถูกต้อง token จะถูกปฏิเสธโดย client ตั้งค่าอย่างชัดเจน:

```
KC_HOSTNAME=https://auth.example.com
```

Keycloak 26 เปลี่ยน option เรื่อง hostname มาเป็น **hostname v2** ความต่างที่สำคัญคือ `KC_HOSTNAME` รับ base URL เต็มรูปแบบได้แล้ว ทั้ง scheme, port และ context path เช่น `https://auth.example.com:8443/auth` ก็ใช้ได้ ถ้าใส่แค่ hostname เปล่า ๆ ก็ยังทำงานได้ โดย scheme และ port จะถูก resolve จาก request

อีกสอง option ที่ควรรู้:

- `KC_HOSTNAME_ADMIN` — ให้ admin console อยู่คนละ URL เพื่อไม่ต้องเปิด console ไว้บน URL สาธารณะ
- `KC_HOSTNAME_STRICT=false` — ให้ Keycloak resolve hostname จาก request header เหมาะกับการทดสอบในเครื่องหลัง proxy แต่ห้ามใช้ใน production

ถ้าเจอ `KC_HOSTNAME_URL`, `KC_HOSTNAME_PORT` หรือ `KC_HOSTNAME_PATH` ในคู่มือเก่า นั่นคือ hostname v1 ซึ่งถูกลบไปแล้วใน 26

### 3. HTTPS และ reverse proxy

Keycloak ต้องให้บริการผ่าน HTTPS มีสองตัวเลือกทั่วไป:

- **Edge termination (แนะนำ):** reverse proxy (Nginx, Traefik, AWS ALB) จัดการ TLS และส่งต่อ plain HTTP ไปยัง Keycloak บอก Keycloak ให้เชื่อถือ proxy header:

  ```
  KC_PROXY_HEADERS=xforwarded
  ```

- **Passthrough:** Keycloak จัดการ TLS เอง ให้ certificate และ private key ผ่าน `KC_HTTPS_CERTIFICATE_FILE` และ `KC_HTTPS_CERTIFICATE_KEY_FILE`

### 4. Optimised build

ใน production ให้รัน `kc.sh build` ก่อน `kc.sh start` ขั้นตอน build คอมไพล์ provider configuration, augment Quarkus app และ cache classpath scanning — ลดเวลา startup จาก ~30 วินาที เป็น ~3 วินาที การ deploy ด้วย Docker ส่วนใหญ่ทำในรูปแบบ multi-stage Dockerfile:

```bash
# Build stage
kc.sh build --db=postgres

# Runtime stage
kc.sh start --optimized
```

## session ไม่หายเมื่อ restart

ตั้งแต่ Keycloak 25 **persistent user session** เป็นค่าเริ่มต้น: session ถูกเขียนลง database และโหลดเข้า cache เมื่อจำเป็น restart node หรือทั้ง cluster ผู้ใช้ก็ยัง login อยู่ นี่เป็นพฤติกรรมที่เปลี่ยนไปจากรุ่นเก่าที่ restart ทีเดียวผู้ใช้หลุดหมด

ถ้าต้องการปิดเพื่อใช้แบบ cache อย่างเดียว:

```
KC_FEATURES_DISABLED=persistent-user-sessions
```

อย่าปิดโดยไม่จำเป็น session แบบ volatile จะหายทั้งหมดเมื่อ node สุดท้าย restart และใช้ร่วมกับฟีเจอร์ `multi-site` ไม่ได้

## Production docker run

นี่คือ `docker run` ขั้นต่ำที่รวมสี่สิ่งที่ต้องมีทั้งหมด:

export const prodRunCode = `docker run -d --name keycloak \\
  -p 8080:8080 \\
  -e KC_DB=postgres \\
  -e KC_DB_URL=jdbc:postgresql://db:5432/keycloak \\
  -e KC_DB_USERNAME=keycloak \\
  -e KC_DB_PASSWORD=\${DB_PASSWORD} \\
  -e KC_HOSTNAME=auth.example.com \\
  -e KC_PROXY_HEADERS=xforwarded \\
  -e KC_BOOTSTRAP_ADMIN_USERNAME=admin \\
  -e KC_BOOTSTRAP_ADMIN_PASSWORD=\${ADMIN_PASSWORD} \\
  quay.io/keycloak/keycloak:latest \\
  start --optimized`;

<CopyCode client:visible code={prodRunCode} label="Copy docker run" />

ส่ง `DB_PASSWORD` และ `ADMIN_PASSWORD` จาก secrets manager ของคุณ — อย่า hardcode ใน command

## Config-as-code ด้วย realm export/import

การคลิกใน admin console ด้วยมือไม่สามารถทำซ้ำได้ Export configuration ของ realm เป็น JSON และ commit ไปยัง version control:

```bash
# Export a realm
/opt/keycloak/bin/kc.sh export --realm my-app --file /tmp/my-app-realm.json

# Import on startup
/opt/keycloak/bin/kc.sh start --import-realm
```

วาง `my-app-realm.json` ไว้ใน `/opt/keycloak/data/import/` แล้ว Keycloak จะ import อัตโนมัติตอน start ครั้งแรก ทำให้ realm configuration สามารถทำซ้ำได้ทุก environment

## กลยุทธ์การ backup

state ของ Keycloak อยู่ใน external database ทั้งหมด Backup database โดยใช้กลไก backup มาตรฐานของ database provider ของคุณ container เองไม่มี state และไม่จำเป็นต้อง backup — เพียงตรวจสอบว่า realm export JSON อยู่ใน version control เป็น baseline ของ configuration

<Callout title="สี่สิ่งที่ต้องมีสำหรับ production">
External database, fixed hostname, HTTPS และ optimised build — `start-dev` ไม่มีสิ่งใดเลย การเปลี่ยนจาก `start-dev` ไปยัง `start --optimized` โดยไม่มีสี่สิ่งนี้จะทำให้ deployment พังทลาย
</Callout>

## ข้อแลกเปลี่ยน

| ตัวเลือก | Benefit | Cost |
|---|---|---|
| Edge TLS termination (reverse proxy) | ดูแล certificate ที่จุดเดียว (Nginx/Traefik/ALB) รองรับ rolling cert renewal ได้ง่าย | ต้องตั้งค่า `KC_PROXY_HEADERS` ให้ถูกต้อง มิฉะนั้น Keycloak จะมองเห็น protocol/host ผิดและ redirect พัง |
| TLS passthrough (Keycloak จัดการเอง) | ไม่ต้องพึ่ง proxy เพิ่ม เหมาะกับ deployment ที่เรียบง่าย | ต้องจัดการ certificate lifecycle เองภายใน Keycloak ทุก instance |
| Optimised build (`kc.sh build` ล่วงหน้า) | ลดเวลา startup จาก ~30 วินาที เหลือ ~3 วินาที เหมาะกับ container ที่ scale ขึ้น-ลงบ่อย | ต้องเพิ่มขั้นตอน build แยกใน CI/CD และ rebuild image ทุกครั้งที่เปลี่ยน provider config |

## ข้อผิดพลาดที่พบบ่อย

- **รัน `start-dev` ใน production** — ยังใช้ embedded H2 database, self-signed certificate และผ่อนปรนการตรวจสอบ hostname ทำให้ข้อมูลหายเมื่อ container restart และเปิดช่องโหว่ด้านความปลอดภัยหลายจุดพร้อมกัน
- **เปิด `/metrics` หรือ `/health` endpoint ออกสู่ public โดยไม่จำกัดการเข้าถึง** — endpoint เหล่านี้เปิดเผยข้อมูล internal ของระบบ ควรจำกัดให้เข้าถึงได้เฉพาะจาก internal network หรือ monitoring system เท่านั้น
- **ไม่ตั้งค่า `KC_HOSTNAME` ให้ตรงกับ domain จริง** — ทำให้ token issuer และ OIDC discovery document ไม่ตรงกับที่ client คาดหวัง client จะปฏิเสธ token แม้ Keycloak ทำงานปกติทุกอย่าง

> 💡 ตัวอย่างจากของจริง
>
> **Production checklist มาตรฐานของทีม platform** — ทีม infrastructure ส่วนใหญ่ผูก external PostgreSQL, fixed hostname ผ่าน DNS, TLS ผ่าน reverse proxy และ multi-node clustering เข้าด้วยกันเป็น deployment template เดียว แล้วใช้ realm export/import เพื่อให้ config เดิมซ้ำได้ทุก environment
>
> **High-availability clustering** — องค์กรที่ต้องการ uptime สูงรัน Keycloak หลาย instance หลัง load balancer โดยแชร์ external database ตัวเดียวกัน ทำให้ instance ใดตายไปหนึ่งตัว session ของผู้ใช้ก็ยังไม่หลุด

export const quizProduction = [
  {
    q: 'เมื่อ restart Keycloak 26 ด้วยค่าเริ่มต้น session ของผู้ใช้จะเป็นอย่างไร?',
    options: [
      'ผู้ใช้หลุดทุกคน เพราะ session อยู่แค่ในหน่วยความจำ',
      'session ยังอยู่ เพราะ persistent user session เป็นค่าเริ่มต้น',
      'session ยังอยู่ก็ต่อเมื่อเปิดฟีเจอร์ multi-site',
      'session ถูกสร้างใหม่จาก access token ที่ client ส่งมา',
    ],
    answer: 1,
  },
  {
    q: 'start-dev ใช้ database ใดเป็นค่าเริ่มต้น?',
    options: [
      'PostgreSQL',
      'MySQL',
      'Embedded H2',
      'SQLite',
    ],
    answer: 2,
  },
  {
    q: 'KC_PROXY_HEADERS=xforwarded บอกอะไรกับ Keycloak?',
    options: [
      'ให้ปิด HTTPS ทั้งหมด',
      'ให้เชื่อถือ X-Forwarded-* header จาก reverse proxy สำหรับตรวจจับ hostname และ protocol',
      'ให้ forward ทุก request ไปยัง external proxy',
      'ให้บังคับใช้ client certificate',
    ],
    answer: 1,
  },
  {
    q: 'ทำไมคุณควรรัน kc.sh build ก่อน kc.sh start ใน production?',
    options: [
      'เพื่อสร้าง schema ของ external database',
      'เพื่อ compile provider configuration และลดเวลา startup',
      'เพื่อสร้าง self-signed TLS certificate',
      'เพื่อ export realm configuration เริ่มต้น',
    ],
    answer: 1,
  },
  {
    q: 'คุณทำให้ Keycloak import realm อัตโนมัติตอน startup ครั้งแรกได้อย่างไร?',
    options: [
      'ตั้งค่า KC_REALM_IMPORT=true',
      'วางไฟล์ realm JSON ไว้ใน /opt/keycloak/data/import/ แล้วรัน start --import-realm',
      'เรียก Admin REST API ก่อนที่ Keycloak จะเริ่มทำงาน',
      'เปิดใช้งาน auto-import event listener',
    ],
    answer: 1,
  },
];

<Quiz client:visible id="admin-theming-production/production-checklist" questions={quizProduction} />

<ProgressTracker client:visible id="admin-theming-production/production-checklist" />
