---
title: Credentials & OTP
description: 'เรียนรู้วิธีตั้งและรีเซ็ต password, ตั้งค่า OTP สำหรับการพิสูจน์ตัวตนสองปัจจัย, และกำหนด password policy ใน Keycloak'
sidebar:
  order: 4
---

import Callout from '../../../../components/Callout.astro';
import CopyCode from '../../../../components/CopyCode.tsx';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';

## แท็บ Credentials

user ทุกคนใน Keycloak มีแท็บ **Credentials** อยู่บนหน้าโปรไฟล์ของพวกเขา นี่คือที่ที่คุณจัดการความลับสำหรับการพิสูจน์ตัวตนที่ผูกกับบัญชีนั้น Keycloak รองรับ credential หลายชนิด:

- **Password** — credential ที่พบบ่อยที่สุด สามารถตั้งเป็นแบบถาวรหรือชั่วคราว (temporary) ได้
- **OTP (One-Time Password)** — second factor แบบอิงเวลา (TOTP) หรืออิงตัวนับ (HOTP) โดยทั่วไปใช้ร่วมกับแอป authenticator เช่น Google Authenticator หรือ Authy

ผู้ดูแลระบบสามารถตั้งหรือรีเซ็ต credential ในนามของ user ได้ และยังสามารถบังคับให้ user ตั้งค่า credential ด้วยตนเองในการล็อกอินครั้งถัดไปได้ด้วย

## การตั้งและรีเซ็ต password

1. ใน admin console คลิก **Users** ในแถบด้านซ้าย
2. ค้นหาและเปิด user ที่คุณต้องการตั้งหรือรีเซ็ต password
3. คลิกแท็บ **Credentials**
4. คลิก **Set password**
5. ใส่ password ใหม่ในช่อง **Password** และยืนยันใน **Password confirmation**
6. สลับสวิตช์ **Temporary** ไปที่ **On** หากคุณต้องการบังคับให้ user เลือก password ใหม่ในครั้งถัดไปที่พวกเขาล็อกอิน ปล่อยไว้ **Off** เพื่อตั้ง password เป็นแบบถาวร
7. คลิก **Save password** เพื่อยืนยัน

flag **Temporary** เป็นความแตกต่างที่สำคัญ: เมื่อตั้งเป็น **On**, Keycloak จะทำเครื่องหมาย credential ว่าต้องการการอัปเดต ครั้งถัดไปที่ user ล็อกอิน พวกเขาจะถูกเปลี่ยนเส้นทางไปยังหน้า "Please update your password" ก่อนที่จะได้รับอนุญาตให้เข้าแอปพลิเคชัน นี่คือรูปแบบที่ถูกต้องสำหรับการรีเซ็ต password ที่ริเริ่มโดย admin — ผู้ดูแลระบบไม่จำเป็นต้องรู้ password ถาวรของ user เลย

## การรีเซ็ต password ผ่าน REST API

export const resetPasswordCode = `curl -s -X PUT -H "Authorization: Bearer \${TOKEN}" -H "Content-Type: application/json" -d '{"type":"password","value":"TempPass123!","temporary":true}' http://localhost:8080/admin/realms/my-app/users/\${USER_ID}/reset-password`;

<CopyCode client:visible code={resetPasswordCode} label="Copy curl" />

แทนที่ `TOKEN` ด้วย admin access token ที่ถูกต้อง และ `USER_ID` ด้วย UUID ของ user (มองเห็นได้ใน URL เมื่อคุณเปิด user ใน admin console) ตั้งค่า `"temporary": false` หากคุณต้องการให้ password เป็นแบบถาวร

<Callout title="Password ชั่วคราว">
ทำเครื่องหมาย password ที่รีเซ็ตว่าเป็น "Temporary" เพื่อบังคับให้ user เปลี่ยนมันในการล็อกอินครั้งถัดไป — อย่าปล่อยให้ password ที่รีเซ็ตซึ่งเป็นที่รู้กันเป็นแบบถาวร password ถาวรที่ผู้ดูแลระบบตั้งไว้คือ credential ที่ผู้ดูแลระบบรู้ ซึ่งเป็นความเสี่ยงด้านความปลอดภัย
</Callout>

## การตั้งค่า OTP / 2FA

Keycloak รองรับการพิสูจน์ตัวตนสองปัจจัยผ่าน OTP การตั้งค่า OTP สำหรับ realm หนึ่งจะถูกกำหนดแบบ global ใน admin console และ user แต่ละรายลงทะเบียนอุปกรณ์ authenticator ของตัวเองผ่าน Account Console

### การตั้งค่า OTP policy สำหรับ realm

1. คลิก **Realm settings** ในแถบด้านซ้าย
2. คลิกแท็บ **Authentication** ในแถบนำทางด้านบน
3. คลิกแท็บ **OTP policy**
4. ตรวจสอบและปรับการตั้งค่าต่อไปนี้:
   - **OTP type** — เลือก **Time Based (TOTP)** (แนะนำ; ใช้งานได้กับ Google Authenticator, Authy, และแอป authenticator ส่วนใหญ่) หรือ **Counter Based (HOTP)**
   - **OTP hash algorithm** — `SHA1` เป็นค่าเริ่มต้นและรองรับโดยแอป authenticator แทบทุกตัว `SHA256` และ `SHA512` ให้การ hash ที่แข็งแกร่งกว่าแต่ต้องการการรองรับจากแอป
   - **Number of digits** — `6` เป็นค่ามาตรฐาน; บางแอปรองรับ `8`
   - **OTP token period** — สำหรับ TOTP, นี่คือจำนวนวินาทีที่แต่ละ code มีผล (ค่าเริ่มต้น: `30`)
   - **Look-ahead window** — จำนวน interval ที่ Keycloak ยอมรับสำหรับความคลาดเคลื่อนของนาฬิกา (clock drift) ระหว่าง server กับอุปกรณ์ของ user
5. คลิก **Save**

### user ลงทะเบียนอุปกรณ์ OTP อย่างไร

user ลงทะเบียนอุปกรณ์ OTP ของตัวเองผ่าน Keycloak Account Console:

1. ไปที่ URL ของ Account Console สำหรับ realm ของคุณ: `http://localhost:8080/realms/my-app/account/`
2. ลงชื่อเข้าใช้ด้วย credential ของ user
3. คลิก **Security** ในเมนูด้านซ้าย
4. คลิก **Two-factor authentication** (หรือ **Signing in** ขึ้นอยู่กับเวอร์ชัน Keycloak)
5. ภายใต้ **Two-factor authenticators** คลิก **Set up authenticator application**
6. สแกน QR code ที่แสดงด้วยแอป authenticator (Google Authenticator, Authy, Microsoft Authenticator ฯลฯ)
7. ใส่รหัส 6 หลักที่แสดงในแอปเพื่อยืนยันอุปกรณ์ แล้วคลิก **Submit**

ตอนนี้อุปกรณ์ได้รับการลงทะเบียนแล้ว ตั้งแต่การล็อกอินครั้งถัดไปเป็นต้นไป Keycloak จะถามรหัส OTP หลังจากขั้นตอน password เมื่อ OTP ถูกกำหนดให้จำเป็นใน authentication flow

## Password policy

Keycloak ให้คุณบังคับใช้กฎ password ในระดับ realm ผ่าน **Password policy** กฎเหล่านี้จะมีผลทุกครั้งที่ user ตั้งหรือเปลี่ยน password ของตน

วิธีตั้งค่า password policy:

1. คลิก **Realm settings** ในแถบด้านซ้าย
2. คลิกแท็บ **Authentication**
3. คลิกแท็บ **Password policy**
4. คลิก **Add policy** และเลือกจากประเภท policy ที่มี:

| Policy | สิ่งที่มันบังคับใช้ |
|---|---|
| Minimum length | password ต้องมีความยาวอย่างน้อย N ตัวอักษร |
| Special characters | password ต้องมีอักขระพิเศษอย่างน้อย N ตัว |
| Uppercase characters | password ต้องมีตัวอักษรพิมพ์ใหญ่อย่างน้อย N ตัว |
| Lowercase characters | password ต้องมีตัวอักษรพิมพ์เล็กอย่างน้อย N ตัว |
| Digits | password ต้องมีตัวเลขอย่างน้อย N ตัว |
| Not recently used | password ต้องไม่ตรงกับ N password ล่าสุดของ user |
| Expire password | password หมดอายุหลังจาก N วัน; user จะถูกบังคับให้เปลี่ยน |
| Password blacklist | password ต้องไม่ปรากฏในไฟล์ blacklist ที่ตั้งค่าไว้ |

5. ตั้งค่าให้กับแต่ละ policy ที่คุณเพิ่ม
6. คลิก **Save**

policy จะถูกบังคับใช้ทันทีสำหรับการเปลี่ยน password ใหม่ทั้งหมด password ที่มีอยู่ก่อนหน้า policy จะไม่ถูกทำให้ใช้ไม่ได้ย้อนหลัง — มีเพียงการตั้งหรือรีเซ็ตใหม่เท่านั้นที่จะถูกตรวจสอบ

export const quizCredentialsOtp = [
  {
    q: 'การเปิด flag "Temporary" เมื่อตั้ง password ทำอะไร?',
    options: [
      'password หมดอายุหลังจาก 24 ชั่วโมงโดยอัตโนมัติ',
      'user ถูกบังคับให้เปลี่ยน password ในการล็อกอินครั้งถัดไป',
      'password ถูกจัดเก็บแบบเข้ารหัสใน cache ชั่วคราว',
      'user ได้รับอีเมลพร้อม password ชั่วคราว',
    ],
    answer: 1,
  },
  {
    q: 'ใน admin console คุณตั้งค่า OTP type, algorithm, และ token period สำหรับ realm ได้ที่ไหน?',
    options: [
      'Users → Credentials → OTP Settings',
      'Clients → Authentication → OTP',
      'Realm settings → Authentication → OTP policy',
      'Authentication → Flows → Browser',
    ],
    answer: 2,
  },
  {
    q: 'user ลงทะเบียนอุปกรณ์ OTP authenticator ของตัวเองใน Keycloak อย่างไร?',
    options: [
      'ผู้ดูแลระบบกำหนดอุปกรณ์ OTP ให้พวกเขาจาก admin console',
      'user ไปที่ Account Console, ไปที่ Security, แล้วตั้งค่าแอป authenticator',
      'user ส่งคำขอทางอีเมลไปยังผู้ดูแลระบบ realm',
      'การลงทะเบียน OTP เกิดขึ้นโดยอัตโนมัติในการล็อกอินครั้งแรก',
    ],
    answer: 1,
  },
  {
    q: 'password policy ใดที่ป้องกันไม่ให้ user นำ password ล่าสุดมาใช้ซ้ำ?',
    options: [
      'Expire password',
      'Minimum length',
      'Password blacklist',
      'Not recently used',
    ],
    answer: 3,
  },
];

<Quiz client:visible id="users-groups-credentials/credentials-otp" questions={quizCredentialsOtp} />

<ProgressTracker client:visible id="users-groups-credentials/credentials-otp" />
