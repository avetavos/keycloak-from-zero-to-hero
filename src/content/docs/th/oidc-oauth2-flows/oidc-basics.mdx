---
title: 'OIDC Discovery Document'
description: 'วิธีค้นหาและอ่าน OIDC discovery document ที่ Keycloak realm ทุกตัวเผยแพร่'
sidebar:
  order: 2
---

import Callout from '../../../../components/Callout.astro';
import CopyCode from '../../../../components/CopyCode.tsx';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';

## Discovery document คืออะไร

Keycloak realm ทุกตัวเผยแพร่ OpenID Connect discovery document ที่ `/.well-known/openid-configuration` ใต้ `/realms/<realm>`

URL เต็ม:

```
https://<keycloak-host>/realms/<realm>/.well-known/openid-configuration
```

นี่คือแหล่งข้อมูลเดียวสำหรับทุก endpoint ที่แอปต้องการ fetch ครั้งเดียวตอนเริ่มต้นและคุณจะได้ทุกอย่าง — authorization endpoint, token endpoint, JWKS URI และอื่น ๆ

## Endpoint สำคัญใน discovery document

| ชื่อ endpoint | JSON key | วัตถุประสงค์ |
|---|---|---|
| Authorization endpoint | `authorization_endpoint` | Redirect ผู้ใช้ไปยังหน้า login |
| Token endpoint | `token_endpoint` | แลก authorization code เป็น token |
| UserInfo endpoint | `userinfo_endpoint` | ส่งคืน claim เกี่ยวกับผู้ใช้ที่ยืนยันตัวตนแล้ว |
| JWKS URI | `jwks_uri` | Public key สำหรับยืนยัน JWT signature |
| End session endpoint | `end_session_endpoint` | Logout ผู้ใช้ |

## การ fetch discovery document

แทนที่ `KC_URL` และ `REALM` ด้วยค่าของคุณ การตอบกลับจะเป็น JSON object ที่มี URL ของ endpoint ทั้งหมดข้างต้น

export const discoveryCode = `curl https://\${KC_URL}/realms/\${REALM}/.well-known/openid-configuration | jq .`;

<CopyCode client:visible code={discoveryCode} label="Copy curl" />

<Callout title="เริ่มจาก discovery document เสมอ">
อย่า hardcode URL ของ endpoint ทุก endpoint ที่แอปต้องการอยู่ใน discovery document เมื่อ Keycloak อัปเกรดหรือย้าย แอปจะรับ URL ใหม่อัตโนมัติหากดึง discovery doc ตอนเริ่มต้น
</Callout>

## Trade-offs

| ตัวเลือก | Benefit | Cost |
|---|---|---|
| ใช้ OIDC discovery document | แอปดึง config อัตโนมัติ ไม่ต้อง hardcode endpoint แต่ละตัว | ต้องมี network call เพิ่มตอน startup และต้อง cache ให้ดีเพื่อไม่ให้ดึงซ้ำทุก request |
| Hardcode endpoint URL เอง | เริ่มต้นเร็ว ไม่ต้องพึ่ง discovery document | เมื่อ Keycloak ย้าย realm หรือเปลี่ยน URL ต้องไปแก้โค้ดทุกที่ที่ hardcode ไว้ |

## Common Mistakes

- **สับสนระหว่าง OAuth2 กับ OIDC** — OAuth2 คือ authorization framework (ให้สิทธิ์เข้าถึงทรัพยากร) ส่วน OIDC เป็น identity layer ที่สร้างต่อบน OAuth2 (บอกว่าใครคือผู้ใช้) ต้องขอ `scope=openid` เท่านั้นถึงจะได้ `id_token` กลับมา
- **ไม่ตรวจสอบ `issuer` ใน discovery document** — ก่อนเชื่อ endpoint ใด ๆ ควร verify ว่า `issuer` ตรงกับ Keycloak realm ที่คาดไว้ ไม่งั้นเสี่ยงต่อการถูกหลอกให้เชื่อ discovery document ปลอม
- **Cache discovery document ไว้ตลอดชีพ ไม่เคย refresh** — ถ้า Keycloak หมุน key หรือย้าย endpoint แอปที่ cache ค่าเก่าไว้ถาวรจะพังแบบไม่มีสัญญาณเตือน ควรตั้ง TTL หรือ refresh เป็นระยะ

> 💡 Real World Example
>
> **ปุ่ม "Log in with Google" หรือ "Log in with GitHub"** — เบื้องหลังคือ OIDC เต็มรูปแบบ แอปจะดึง discovery document ของ Google/GitHub ก่อนเพื่อรู้ authorization endpoint และ token endpoint แล้วค่อยเริ่ม flow
>
> **Auth0 และ Okta** — เป็น Identity Provider (IdP) เชิงพาณิชย์ที่ใช้ pattern discovery document แบบเดียวกับ Keycloak ทำให้แอปสลับ provider ได้โดยแทบไม่ต้องแก้โค้ด เพราะทุกตัวพูดภาษา OIDC เดียวกัน

export const quizOidcBasics = [
  {
    q: 'Keycloak เผยแพร่ OIDC discovery document ที่ไหน?',
    options: [
      '/.well-known/openid-configuration ใต้ /realms/<realm>',
      '/admin/master/console',
      '/auth/admin/realms',
      '/.well-known/jwks.json',
    ],
    answer: 0,
  },
  {
    q: 'JSON key ใดใน discovery document ให้ token endpoint?',
    options: [
      'authorization_endpoint',
      'token_endpoint',
      'userinfo_endpoint',
      'jwks_uri',
    ],
    answer: 1,
  },
  {
    q: 'JWKS URI ใช้ทำอะไร?',
    options: [
      'Logout ผู้ใช้',
      'เก็บข้อมูลโปรไฟล์ผู้ใช้',
      'ให้ public key สำหรับยืนยัน JWT signature',
      'ออก refresh token',
    ],
    answer: 2,
  },
  {
    q: 'เหตุใดแอปควร fetch discovery document แทนการ hardcode endpoint?',
    options: [
      'เร็วกว่า hardcode',
      'Keycloak อาจย้าย endpoint ข้ามเวอร์ชัน discovery ทำให้แอปรับ URL ใหม่อัตโนมัติ',
      'URL ที่ hardcode ถูก CORS บล็อก',
      'Token endpoint เปลี่ยนทุก login',
    ],
    answer: 1,
  },
];

<Quiz client:visible id="oidc-oauth2-flows/oidc-basics" questions={quizOidcBasics} />

<ProgressTracker client:visible id="oidc-oauth2-flows/oidc-basics" />
