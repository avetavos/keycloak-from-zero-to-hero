---
title: 'Client Credentials Grant'
description: 'ใช้ Client Credentials grant สำหรับการเรียก machine-to-machine (M2M) ที่ไม่มีผู้ใช้เข้ามาเกี่ยวข้อง'
sidebar:
  order: 4
---

import Callout from '../../../../components/Callout.astro';
import CopyCode from '../../../../components/CopyCode.tsx';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';

## Client Credentials grant คืออะไร?

Client Credentials grant ถูกออกแบบมาสำหรับการเรียก machine-to-machine ที่ไม่มีผู้ใช้ แทนที่จะให้ผู้ใช้ยืนยันตัวตน client จะยืนยันตัวตนตัวเองด้วย `client_id` และ `client_secret` เนื่องจากไม่มีผู้ใช้ใน flow Keycloak จึงส่งคืนเฉพาะ access token — ไม่มี ID token

grant นี้เป็นส่วนหนึ่งของ OAuth2 specification และเป็นวิธีมาตรฐานสำหรับ service account, background job, และกระบวนการอัตโนมัติที่ต้องเรียก protected API

## เมื่อไรควรใช้

ใช้ Client Credentials grant สำหรับ:

- Background job ที่เรียก internal API
- Microservice ที่เรียกกันเอง
- CI/CD pipeline ที่จัดการ infrastructure

**ไม่ควรใช้** กับสิ่งใดก็ตามที่มีผู้ใช้จริงเข้ามาเกี่ยวข้อง หากผู้ใช้ต้องยืนยันตัวตนและแอปต้องทำงานในนามของพวกเขา ให้ใช้ Authorization Code + PKCE แทน

## การตั้งค่าใน Keycloak

1. สร้าง confidential client (ตั้ง **Client authentication** เป็น **On**)
2. เปิดใช้ **Service Accounts** — บน Capability config tab เปิดใช้ **Service account roles**
3. คัดลอก client secret จาก **Credentials** tab
4. กำหนด realm role หรือ client role ให้กับ service account user บน **Service Accounts Roles** tab หาก API ของคุณตรวจสอบ role ใน token

## การขอ token

export const clientCredsCode = `curl -X POST https://\${KC_URL}/realms/\${REALM}/protocol/openid-connect/token \\
  -d "grant_type=client_credentials" \\
  -d "client_id=\${CLIENT_ID}" \\
  -d "client_secret=\${CLIENT_SECRET}"`;

<CopyCode client:visible code={clientCredsCode} label="Copy curl" />

<Callout title="ไม่มีผู้ใช้ ไม่มี ID token">
Client Credentials grant ใช้สำหรับ service-to-service — ไม่มีผู้ใช้ดังนั้น Keycloak จึงไม่ออก ID token คุณได้รับเฉพาะ access token เท่านั้น หากต้องการทำงานในนามของผู้ใช้คนใดคนหนึ่ง ให้ใช้ Authorization Code + PKCE แทน
</Callout>

export const quizClientCreds = [
  {
    q: 'ควรใช้ Client Credentials grant เมื่อใด?',
    options: [
      'เมื่อผู้ใช้ login ผ่านเบราว์เซอร์',
      'สำหรับการเรียก machine-to-machine ที่ไม่มีผู้ใช้',
      'สำหรับแอปมือถือที่ใช้ PKCE',
      'เมื่อต้องการ ID token',
    ],
    answer: 1,
  },
  {
    q: 'Keycloak ส่งอะไรกลับมาเมื่อ Client Credentials request สำเร็จ?',
    options: [
      'ID token และ access token',
      'เฉพาะ access token (ไม่มี ID token)',
      'Refresh token เท่านั้น',
      'Authorization code',
    ],
    answer: 1,
  },
  {
    q: 'การตั้งค่าใดของ Keycloak ที่ต้องเปิดใช้สำหรับ Client Credentials?',
    options: [
      'Valid redirect URIs',
      'Web origins',
      'Service account roles',
      'PKCE method',
    ],
    answer: 2,
  },
  {
    q: 'Client ประเภทใดที่จำเป็นสำหรับ Client Credentials?',
    options: [
      'Public client',
      'Confidential client (Client authentication = On)',
      'SAML client',
      'Device client',
    ],
    answer: 1,
  },
];

<Quiz client:visible id="oidc-oauth2-flows/client-credentials" questions={quizClientCreds} />

<ProgressTracker client:visible id="oidc-oauth2-flows/client-credentials" />
