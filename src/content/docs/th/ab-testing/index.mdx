---
title: 'A/B Test คืออะไร?'
description: 'เรียนรู้ว่า A/B test คืออะไรใน GrowthBook วิธีที่ feature-flag experiment ต่างจาก Experiments UI และ lifecycle ทั้งหมดของ experiment'
sidebar:
  order: 1
---

import CopyCode from '../../../../components/CopyCode.tsx';
import Callout from '../../../../components/Callout.astro';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';

## A/B Test คืออะไร?

A/B test หรือที่เรียกว่า experiment คือวิธีการเปรียบเทียบ variation สองแบบขึ้นไปของประสบการณ์ใช้งานผลิตภัณฑ์อย่างมีการควบคุม เพื่อหาว่าแบบไหนบรรลุผลลัพธ์ที่ต้องการได้ดีกว่ากัน แทนที่จะเดาเอาเอง คุณปล่อยให้พฤติกรรมของผู้ใช้จริงเป็นตัวบอก

ใน GrowthBook ทุก experiment เริ่มต้นด้วย hypothesis: ข้อความที่สามารถพิสูจน์ได้ เช่น "การเปลี่ยนสีปุ่ม call-to-action จากสีเทาเป็นสีเขียวจะเพิ่ม click-through rate" จากนั้นคุณกำหนด variation (ต้นฉบับ control และ treatment อย่างน้อยหนึ่งแบบ) แบ่ง traffic ระหว่างกัน เก็บข้อมูล metric และใช้การวิเคราะห์ทางสถิติเพื่อตัดสินใจอย่างมั่นใจ

### Feature-flag experiment กับ Experiments UI

GrowthBook มีสองแนวทางในการรัน experiment:

**Feature-flag experiment** เชื่อมต่อ experiment เข้ากับ feature flag โดยตรง โค้ดของคุณเรียก `gb.getFeatureValue('experiment-key', 'control')` อยู่แล้วเพื่ออ่านฟีเจอร์ GrowthBook จะแอบ assign ผู้ใช้แต่ละคนเข้า variation bucket และบันทึก exposure ไว้ แนวทางนี้แยก experimentation logic ออกจาก UI และเป็นแนวทางที่แนะนำสำหรับทีม product และ engineering

**Experiments UI** ให้คุณกำหนด experiment โดยไม่ต้องแตะ feature flag เหมาะสำหรับ visual หรือ content experiment ที่บริหารผ่าน interface แบบ no-code ภายใต้นั้นยังคงใช้ engine การ assignment และ metric เดียวกัน

ทั้งสองแนวทางใช้การวิเคราะห์ทางสถิติที่เหมือนกัน หมายความว่าคุณสามารถเปรียบเทียบผลลัพธ์และรวมความรู้ทั่วทั้งโปรแกรมได้

### Lifecycle ของ experiment

ทุก experiment ใน GrowthBook ผ่านห้าขั้นตอน:

1. **Hypothesis** — ระบุอย่างชัดเจนว่าคุณเชื่อว่าจะเกิดอะไรขึ้นและทำไม hypothesis ที่ดีต้องระบุการเปลี่ยนแปลง ทิศทางที่คาดหวัง และ metric ที่จะได้รับผลกระทบ
2. **Variations** — กำหนด control (ประสบการณ์ปัจจุบัน) และ variation treatment อย่างน้อยหนึ่งแบบ ควรเปลี่ยนแปลงให้เฉพาะเจาะจง: การทดสอบหลายอย่างพร้อมกันทำให้ไม่รู้ว่าอะไรเป็นตัวขับเคลื่อนผลลัพธ์
3. **Assignment** — GrowthBook assign ผู้ใช้แต่ละคนเข้า variation เดียวโดยใช้ deterministic hash ของ user identifier ที่เสถียร (เช่น user ID หรือ anonymous device ID) ซึ่งหมายความว่าผู้ใช้จะเห็น variation เดิมทุกครั้งโดยไม่ต้องเก็บข้อมูลฝั่ง server
4. **Metrics** — เชื่อมต่อ data source และกำหนด goal metric (เช่น conversion rate, revenue per user) และ guardrail metric (เช่น page load time) ที่ต้องการตรวจสอบไม่ให้ถดถอย
5. **Decision** — เมื่อถึง statistical significance หรือ sample size ที่วางแผนไว้ GrowthBook จะแสดงผู้ชนะ จากนั้นคุณ ship variation ที่ชนะไปยังผู้ใช้ 100% และ archive experiment

## อ่านค่า variation ที่ถูก assign ในโค้ด

snippet ด้านล่างแสดงรูปแบบขั้นต่ำ: initialize SDK แล้วเรียก `getFeatureValue` พร้อม experiment feature key และ fallback value เป็น `'control'` จากนั้น branch ตามผลลัพธ์เพื่อส่งมอบประสบการณ์ที่ถูกต้อง

export const indexCode = `import { GrowthBook } from '@growthbook/growthbook';

const gb = new GrowthBook({
  apiHost: 'https://cdn.growthbook.io',
  clientKey: 'sdk-YOUR_KEY',
  // Pass stable attributes so assignment is deterministic
  attributes: {
    id: currentUser.id,
  },
});

await gb.init({ timeout: 2000 });

// Returns the variation assigned to this user, or 'control' as fallback
const variation = gb.getFeatureValue('experiment-key', 'control');

if (variation === 'treatment') {
  // Show the new experience
  renderTreatment();
} else {
  // Show the original control experience
  renderControl();
}`;

<CopyCode client:visible code={indexCode} />

<Callout title="จุดสำคัญ">
Experiment แบ่งผู้ใช้ออกเป็น variation ต่างๆ และวัดผลกระทบต่อ metric ของคุณ
</Callout>

export const quizAbTestingIndex = [
  {
    q: 'จุดประสงค์หลักของ A/B test คืออะไร?',
    options: [
      'เพื่อ deploy feature ได้เร็วขึ้นโดยไม่ต้องทดสอบ',
      'เพื่อเปรียบเทียบ variation และหาว่าแบบไหนบรรลุผลลัพธ์ที่ต้องการได้ดีกว่า',
      'เพื่อแทนที่ unit test ใน CI pipeline',
      'เพื่อ assign ผู้ใช้ทุกคนให้เห็นประสบการณ์เดียวกัน',
    ],
    answer: 1,
  },
  {
    q: 'ในภาษา GrowthBook "variation" หมายถึงอะไร?',
    options: [
      'ค่า threshold ของ statistical confidence',
      'หนึ่งในประสบการณ์ที่แตกต่างกันที่ผู้ใช้ถูก assign เข้าไปใน experiment',
      'data source ที่เชื่อมต่อกับ GrowthBook',
      'guardrail metric ที่ป้องกันการถดถอย',
    ],
    answer: 1,
  },
  {
    q: 'ลำดับใดที่อธิบาย lifecycle ของ experiment ใน GrowthBook ได้ถูกต้อง?',
    options: [
      'Variations → Metrics → Hypothesis → Assignment → Decision',
      'Assignment → Hypothesis → Variations → Decision → Metrics',
      'Hypothesis → Variations → Assignment → Metrics → Decision',
      'Metrics → Assignment → Hypothesis → Variations → Decision',
    ],
    answer: 2,
  },
];

<Quiz client:visible id="ab-testing/index" questions={quizAbTestingIndex} />

<ProgressTracker client:visible id="ab-testing/index" />
