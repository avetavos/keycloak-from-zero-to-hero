---
title: 'Flag Types'
description: 'The four GrowthBook value types — boolean, string, number, and JSON — and when to use each.'
sidebar:
  order: 3
---

import CopyCode from '../../../../components/CopyCode.tsx';
import Callout from '../../../../components/Callout.astro';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';

## The four value types

When you create a Feature in GrowthBook you choose one of four **value types**. The type is permanent — you cannot change it after creation, so choose deliberately.

### Boolean

The simplest type: `true` or `false`.

Use it when you need a simple on/off gate — show a new UI, enable a payment method, unlock a beta feature.

```js
if (gb.isOn('dark-mode')) {
  enableDarkMode();
}

// or with explicit fallback:
const isDark = gb.getFeatureValue('dark-mode', false);
```

### String

A text value returned as-is from the SDK.

Use it when you need to vary copy, pick a variant name, or return a configuration string.

```js
const buttonLabel = gb.getFeatureValue('cta-text', 'Get started');
// returns 'Get started', 'Try for free', 'Sign up now', etc.
```

### Number

A numeric value (integer or float).

Use it when you want to tune a threshold, price, limit, or percentage without a code deploy.

```js
const maxItems = gb.getFeatureValue('cart-max-items', 10);
// returns 10, 20, 50, etc.
```

### JSON

An arbitrary JSON object or array.

Use it when you need to ship a whole configuration blob — button colours, pricing tiers, UI layout options — as a single flag.

```js
const theme = gb.getFeatureValue('ui-theme', { color: 'blue', size: 'md' });
// theme.color, theme.size come from GrowthBook
```

The fallback argument to `getFeatureValue` must be a valid JS object that matches the shape you expect from GrowthBook. If GrowthBook is unreachable, your app still works with the fallback.

## Comparing the types at a glance

| Type | SDK return | Best for |
|---|---|---|
| Boolean | `true` / `false` | Feature gates, kill switches |
| String | any string | Copy variants, layout names |
| Number | any number | Thresholds, limits, prices |
| JSON | any object or array | Config bundles, multi-property settings |

## Hoisted SDK snippet — JSON flag

export const jsonFlagCode = `import { GrowthBook } from '@growthbook/growthbook';

const gb = new GrowthBook({
  apiHost: 'https://cdn.growthbook.io',
  clientKey: 'sdk-YOUR_DEV_KEY',
});

await gb.init({ timeout: 2000 });

// JSON flag — always pass a complete fallback object
const config = gb.getFeatureValue('ui-theme', { color: 'blue', size: 'md' });

console.log(config.color); // 'blue' or whatever GrowthBook returns
console.log(config.size);  // 'md' or whatever GrowthBook returns`;

<CopyCode client:visible code={jsonFlagCode} label="Copy JSON flag snippet" />

<Callout title="Key point">
JSON flags let you ship configuration, not just on/off switches. Instead of deploying five separate flags for a pricing page redesign, put all the options in one JSON flag and change them atomically in GrowthBook.
</Callout>

export const quizFlagTypes = [
  {
    q: 'Which value type should you use for a simple feature gate (show/hide a UI element)?',
    options: [
      'String',
      'Number',
      'Boolean',
      'JSON',
    ],
    answer: 2,
  },
  {
    q: 'What does gb.getFeatureValue("cart-max-items", 10) return when GrowthBook is unreachable?',
    options: [
      'null',
      'undefined',
      '0',
      '10',
    ],
    answer: 3,
  },
  {
    q: 'Can you change a Feature\'s value type after it has been created in GrowthBook?',
    options: [
      'Yes, at any time from the Feature settings',
      'No, the value type is permanent',
      'Yes, but only for boolean to string',
      'Yes, but only in development environments',
    ],
    answer: 1,
  },
  {
    q: 'Which value type is best for shipping a multi-property configuration (e.g. colour, size, layout)?',
    options: [
      'String',
      'Boolean',
      'Number',
      'JSON',
    ],
    answer: 3,
  },
];

<Quiz client:visible id="feature-flags/flag-types" questions={quizFlagTypes} />

<ProgressTracker client:visible id="feature-flags/flag-types" />
