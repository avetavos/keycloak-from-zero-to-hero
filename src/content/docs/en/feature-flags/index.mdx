---
title: 'Feature Flags: Overview'
description: 'What a feature flag is in GrowthBook, what this module covers, and a first taste of the SDK.'
sidebar:
  order: 1
---

import RunGrowthBook from '../../../../components/RunGrowthBook.astro';
import CopyCode from '../../../../components/CopyCode.tsx';
import Callout from '../../../../components/Callout.astro';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';

## What is a Feature Flag?

A **feature flag** (also called a feature toggle) is a decision point in your code that lets you turn behaviour on or off without deploying new code. Instead of shipping a risky feature to everyone at once, you wrap it in a flag and control it from a dashboard.

In GrowthBook, a feature flag is called a **Feature**. Every Feature has:

- A **key** — the stable string your code uses to look it up (e.g. `new-checkout`).
- A **value type** — boolean, string, number, or JSON.
- A **default value** — what the SDK returns when no rule matches.
- **Rules** per environment — who sees which value, and when.

<RunGrowthBook />

## What this module covers

| Lesson | What you will learn |
|---|---|
| Create a Flag | How to create a Feature in the GrowthBook UI and read it via the SDK |
| Flag Types | Boolean, string, number, and JSON value types — when to use each |
| Environments | Dev / staging / production — independent flag states and SDK keys |
| Default Values | What the SDK returns when no rule matches, and why a safe fallback matters |

## A first taste: reading a flag in code

GrowthBook ships SDKs for every major language and framework. Here is the minimal Node.js pattern:

```js
import { GrowthBook } from '@growthbook/growthbook';

const gb = new GrowthBook({
  apiHost: 'https://cdn.growthbook.io',
  clientKey: 'sdk-abc123',
});

await gb.init({ timeout: 2000 });

if (gb.isOn('new-checkout')) {
  // show the new checkout flow
} else {
  // show the current checkout flow
}
```

The call `gb.isOn('new-checkout')` asks GrowthBook: "is the `new-checkout` flag on for the current user context?" The answer comes from the rules you configure in the GrowthBook UI — your code never hard-codes it.

export const firstTasteCode = `import { GrowthBook } from '@growthbook/growthbook';

const gb = new GrowthBook({
  apiHost: 'https://cdn.growthbook.io',
  clientKey: 'sdk-abc123',
});

await gb.init({ timeout: 2000 });

if (gb.isOn('new-checkout')) {
  // new checkout flow
} else {
  // current checkout flow
}`;

<CopyCode client:visible code={firstTasteCode} label="Copy SDK init" />

<Callout title="Key point">
A flag's value is decided server-side by its rules and read in your app via the SDK. Your code stays the same — only the rules in GrowthBook change.
</Callout>

export const quizIndex = [
  {
    q: 'What is a GrowthBook Feature key used for?',
    options: [
      'It is the stable string your code uses to look up a flag',
      'It is a secret API password',
      'It determines which SDK version to use',
      'It sets the feature deployment environment',
    ],
    answer: 0,
  },
  {
    q: 'What does gb.isOn("flag-name") return when no rule matches and the default value is false?',
    options: [
      'true',
      'false',
      'null',
      'undefined',
    ],
    answer: 1,
  },
  {
    q: 'Which component shows the Docker Compose setup reminder in this course?',
    options: [
      'CopyCode',
      'Callout',
      'RunGrowthBook',
      'ProgressTracker',
    ],
    answer: 2,
  },
];

<Quiz client:visible id="feature-flags/index" questions={quizIndex} />

<ProgressTracker client:visible id="feature-flags/index" />
