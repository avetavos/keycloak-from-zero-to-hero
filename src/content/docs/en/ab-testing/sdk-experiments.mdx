---
title: 'Running Experiments with the SDK'
description: 'Run an inline experiment with gb.run(), read the result object, and understand when to use gb.run() vs feature-flag-driven experiments.'
sidebar:
  order: 4
---

import CopyCode from '../../../../components/CopyCode.tsx';
import Callout from '../../../../components/Callout.astro';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';

## Running an experiment directly in code

GrowthBook gives you two ways to run an A/B experiment:

1. **Feature-flag experiments** — you create a Feature in the GrowthBook UI, attach an Experiment rule, and read the variation with `gb.getFeatureValue()`. The assignment logic lives in GrowthBook.
2. **Inline experiments with `gb.run()`** — you define the entire experiment in your code and pass it to the SDK. No feature rule needed in the UI.

`gb.run()` is useful when you want to experiment on something that does not map cleanly to a feature flag, or when you are prototyping quickly and do not need a full GrowthBook UI setup.

### The result object

`gb.run()` returns a result object with three key properties:

| Property | Type | Description |
|---|---|---|
| `result.value` | string / number / boolean | The variation value assigned to the current user |
| `result.inExperiment` | boolean | `true` if the user was successfully assigned to a variation bucket |
| `result.variationId` | number | The 0-based index of the assigned variation |

When `result.inExperiment` is `false`, the user fell outside the experiment (e.g. traffic coverage is less than 100 %, or the experiment is stopped). In that case `result.value` is always the first element in the `variations` array — your safe default.

### Inline experiment example

The snippet below initialises the SDK and calls `gb.run()` to assign a user to a button-colour variation:

export const sdkRunCode = `import { GrowthBook } from '@growthbook/growthbook';

const gb = new GrowthBook({
  apiHost: 'https://cdn.growthbook.io',
  clientKey: 'sdk-YOUR_KEY',
});

await gb.init({ timeout: 2000 });

const result = gb.run({
  key: 'button-color-test',
  variations: ['blue', 'green'],
});

if (result.inExperiment) {
  console.log('Variation:', result.value);
  // result.value is 'blue' or 'green'
} else {
  // User is not in the experiment — use default
  console.log('Not in experiment');
}`;

<CopyCode client:visible code={sdkRunCode} />

<Callout title="Key point">
`gb.run()` lets you define an experiment directly in code when you don't want a feature rule.
</Callout>

export const quizSdkExperiments = [
  {
    q: 'What does gb.run() return?',
    options: [
      'A boolean indicating whether the experiment is running',
      'A result object with value, inExperiment, and variationId properties',
      'The index of the winning variation after statistical analysis',
      'A Promise that resolves once the experiment data is fetched',
    ],
    answer: 1,
  },
  {
    q: 'What is the main difference between gb.run() and a feature-flag experiment?',
    options: [
      'gb.run() requires a data source; feature-flag experiments do not',
      'gb.run() defines the experiment entirely in code without a GrowthBook UI feature rule, while feature-flag experiments are driven by rules configured in the UI',
      'Feature-flag experiments support more than two variations; gb.run() is limited to two',
      'gb.run() always includes 100 % of traffic; feature-flag experiments can limit coverage',
    ],
    answer: 1,
  },
  {
    q: 'What does result.inExperiment being false mean?',
    options: [
      'The experiment key was not found in GrowthBook',
      'The user was not assigned to any variation bucket — for example because they fall outside the traffic coverage',
      'The SDK has not finished initialising yet',
      'The experiment has already been stopped and a winner declared',
    ],
    answer: 1,
  },
];

<Quiz client:visible id="ab-testing/sdk-experiments" questions={quizSdkExperiments} />

<ProgressTracker client:visible id="ab-testing/sdk-experiments" />
