---
title: 'Targeting Attributes'
description: 'Define user attributes in Settings → Attributes and pass them to the SDK with setAttributes so targeting rules can evaluate them.'
sidebar:
  order: 2
---

import CopyCode from '../../../../components/CopyCode.tsx';
import Callout from '../../../../components/Callout.astro';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';

## What are Targeting Attributes?

Targeting attributes are key-value pairs that describe the current user or session — for example `id`, `email`, `country`, or `plan`. When the SDK evaluates a flag, GrowthBook compares the current user's attributes against the conditions on each rule to decide which rule (if any) applies.

Attributes are entirely defined by you. GrowthBook has no built-in user model — you tell it what to know about each user by calling `setAttributes`.

### Step 1 — Define attributes in GrowthBook

Before the SDK can match on an attribute, GrowthBook needs to know it exists and what type it holds.

1. In the GrowthBook sidebar, click **Settings → Attributes**.
2. Click **Add Attribute**.
3. Enter the **Attribute name** (e.g. `country`).
4. Select the **Data Type** — `string`, `number`, `boolean`, `secureString`, or `string[]`.
5. Check **Identifier** if this attribute uniquely identifies a user (used as the hash key for bucketing in experiments and percentage rollouts).
6. Click **Save**.

Repeat for every attribute your rules will reference.

### Step 2 — Pass attributes to the SDK

Call `setAttributes` before evaluating any flag. GrowthBook uses whatever is set at evaluation time, so call it as early as possible in your request or session lifecycle.

```js
import { GrowthBook } from '@growthbook/growthbook';

const gb = new GrowthBook({
  apiHost: 'https://cdn.growthbook.io',
  clientKey: 'sdk-YOUR_DEV_KEY',
});

await gb.init({ timeout: 2000 });

gb.setAttributes({
  id: 'user-123',
  email: 'alice@example.com',
  country: 'TH',
  plan: 'pro',
});
```

export const setAttributesCode = `import { GrowthBook } from '@growthbook/growthbook';

const gb = new GrowthBook({
  apiHost: 'https://cdn.growthbook.io',
  clientKey: 'sdk-YOUR_DEV_KEY',
});

await gb.init({ timeout: 2000 });

gb.setAttributes({
  id: 'user-123',
  email: 'alice@example.com',
  country: 'TH',
  plan: 'pro',
});`;

<CopyCode client:visible code={setAttributesCode} label="Copy setAttributes" />

### Identifier vs non-identifier attributes

**Identifier attributes** (such as `id` or `deviceId`) are used as the hash key when bucketing users in percentage rollouts and A/B tests. Checking the Identifier box in Settings tells GrowthBook this attribute can be used for stable, deterministic bucketing — the same user always lands in the same bucket.

**Non-identifier attributes** (such as `country` or `plan`) are used only for targeting condition matching. They filter which users a rule applies to, but they are not used to assign users to experiment variants.

<Callout title="Targeting only works on attributes you set">
If your code never calls `setAttributes` with `country`, any rule that targets `country` will never match. Always keep your SDK attributes in sync with what you define in Settings → Attributes.
</Callout>

export const quizAttributes = [
  {
    q: 'What is the purpose of calling `gb.setAttributes(...)` in the SDK?',
    options: [
      'It uploads user data to the GrowthBook cloud dashboard',
      'It tells the SDK who the current user is so targeting rules can be evaluated against their attributes',
      'It creates new attribute definitions in Settings → Attributes',
      'It reloads the feature flag configuration from the CDN',
    ],
    answer: 1,
  },
  {
    q: 'What is an "Identifier" attribute used for in GrowthBook?',
    options: [
      'It is displayed in the GrowthBook UI as the user\'s display name',
      'It is used as the hash key for bucketing users in percentage rollouts and A/B tests',
      'It is required for every attribute — all attributes must be identifiers',
      'It enables PII masking for GDPR compliance',
    ],
    answer: 1,
  },
  {
    q: 'Where do you define the attributes that GrowthBook rules can target?',
    options: [
      'Directly in the SDK `GrowthBook` constructor options',
      'In each Feature\'s rule editor under the "Attributes" tab',
      'In the GrowthBook UI under Settings → Attributes',
      'In a JSON file committed to your repository',
    ],
    answer: 2,
  },
];

<Quiz client:visible id="targeting-rollouts/attributes" questions={quizAttributes} />

<ProgressTracker client:visible id="targeting-rollouts/attributes" />
